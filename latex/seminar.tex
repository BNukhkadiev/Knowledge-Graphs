\documentclass[a4paper,oneside,bibliography=totoc]{scrartcl}
% Do not change document class, margins, fonts, etc.

% some useful packages (add more as needed)
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{latexsym}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{natbib}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{algorithm} % you can modify the algorithm style to your liking
\usepackage{algorithmic}
\usepackage{csquotes}
\renewcommand{\algorithmiccomment}[1]{\hfill\textit{// #1}}
\usepackage[usenames,dvipsnames]{xcolor}
\usepackage[colorlinks,citecolor=Green]{hyperref} % you may change/remove the colors
\usepackage{lipsum} % you do not need this

\bibliographystyle{chicagoa}
\setcitestyle{authoryear,round,semicolon,aysep={},yysep={,}} \let\cite\citep

% example enviroments (add more as needed)
\newtheorem{definition}{Definition} \newtheorem{proposition}{Proposition}

\begin{document}

\subject{Personal Survey} % change to appropriate type
\title{Overview of Knowledge Graphs}
\author{Bagautdin Nukhkadiev}
\date{\today}
\maketitle

\begin{abstract}

Here goes the abstract. It should be a concise summary of the paper, including the main motivation, methods, results, and conclusions. 
The abstract should be no more than 250 words and should be self-contained, meaning that it should make sense to a reader who has not read the rest of the paper.

\end{abstract}

\section{Introduction}
\label{ch:intro}

Intro here. 


\section{Literature Review}
\label{sec:related_work}

\subsection{Graph Theory}
% Graphs Definition
Graph Kernels - Graph kernels are a class of methods that let you measure similarity 
between graphs accounting for their structure.

\paragraph{Weisfeiler-Lehman graph kernel algorithm.} 


\subsection{Knowledge Graphs}

Knowledge Graphs are structures made to represent expressive logic and knowledge in a graph. 
Currently Knowledge Graphs are a hype topic that has been on a rise in the last couple of years due to rise in fame of intelligent systems such as LLMs.
Today KGs are used in a range of applications, e.g. enrichment of search results or accurate information injection into LLMs.
To start off, let's walk through the basics of Knowledge Graphs (KGs). 

% Cover the idea of KGs 
The idea of a Knowledge Graph is quite simple: information is structured into triplets called facts, 
e.g. (Albert Einstein, BornIn, German Empire). Elements of these triplets are called (head, relation, tail) or (subject, predicate, object).

% Get into RDF


% OWL 1, OWL 2


% RDFs 

% KG embeddings
\subsection{Knowledge Graph Embeddings}
The notion of embedding information into vector/tensor representations is not new. 
Embeddings play central role in NLP and Information retrieval. 
Thus, embedding Knowledge Graphs seems natural and essential for downstream applications. 
Immediately, we ask ourselves a question:
a) how do we embed the graph structure into a vector
b) how do we ensure that these embedding vectors are able to represent the same level of logic expressivity.
c) how do we measure that. 


KG embedding research worked a lot on building models within different Representation Spaces.
\cite{ji_survey_2022} points out 4 representation spaces: Point-Wise, Complex Vector, Gaussian Distribution and Manifold Space.
\paragraph{Point-Wise Space.} Real number space, basically a go-to usual approach for any algorithm. 
Works like TransE, TransR, HAKE, NTN, DistMult go here

\paragraph{Gaussian Distribution.} Very interesting space, not sure why it is only Gaussian. 
It could be just probabilistic. But the idea that vectors learned are not deterministic but random is great
and I presume along with Point-Wise space should have SOTA models. 


% RDF2vec 
\subsection{RDF2Vec}
As an inspiration from NLP, a Word2Vec embedding method was proposed. There is an idea that h+p=t should hold, thus it makes sense to adapt Word2Vec model proposed by \cite{mikolov_efficient_2013} for KG setting.
This is the motivation for RDF2Vec \cite{groth_rdf2vec_2016}. 

\subsubsection{Graph Walks}
Unfortunately, graphs are not as easy to process as natural language. For that reason a preprocessing step generating sequences is required. 
For that RDF2Vec proposes performing walks on Knowledge Graphs. Original approach suggests Random Walks and Weisfeiler-Lehman Kernels as ways to generate walk sequences. 

\paragraph{Random Walks.}

\paragraph{WL Graph Kernel Sequences.} The walks generated by WL graph kernels are shown in base paper to perform best on AIFB, MUTAG and BGS datasets. 
Such model is called K2V SG because it uses kernel walks and skip-gram model. 

\paragraph{Other Walks.} That point is questionable as to why not use BFS. In this work we shall explore that. 


\subsubsection{Implementations of RDF2Vec}

There are only a couple of implementations of RDF2Vec I've found on the Internet. jRDF2Vec developed by DWS Group and PyRDF2Vec developed by PreDiCT.IDLab. 

\paragraph{jRDF2Vec.} Poorly made package that mixes python and java for no reason. 
Java is used for generating random walks. Then, for some unknown reason a Flask server is created. 
Java application sends the walks file to the server endpoint. 
Server backend executes Word2Vec model from Gensim package.

I find this package to be awfuly designed for 2 main reasons: (i) Redundancy - Flask server, Python and Java are all used for no reason whatsoever. 
(ii) Module does not include essential part of original RDF2Vec paper - Weisfeiler-Lehman graph kernels. 

\paragraph{pyRDF2Vec.} This package is more comprehensive and deserves more attention. 
It has several types of Walks including Weisfeiler-Lehman graph kernels. 
In terms of models it has Gensim's Word2Vec and Fasttext.

Neither of these packages have evaluation or analysis scripts. 



% RDF2vecoa (order-aware)
% Keeps track of where in the path a relation appears
% So it distinguishes:
% direct edge
% vs edge appearing later in a path


\subsection{DLCC}
DLCC is a benchmark dataset for evaluating the performance of RDF2Vec models. 
DLCC introduces a dataset covering 12 test cases that represent a wide range of challenges for RDF2Vec models. 

% Describe the test cases in more detail. 
% Put visualizations of the test cases.
% Use Graphy to visualize the test cases.


\section{Graph Walks}

Walk on graphs are are used for several purposes. 


\section{Graph Sampling}
Sampling for graphs is a technique used to reduce the size of a graph while preserving its properties. 
It is often used in the context of large graphs, where it is not feasible to process the entire graph.



\section{Evaluating RDF2Vec on DLCC}
RDF2Vec models have demonstrated significant potential on benchmark datasets such as DLCC. 
That said, they have not yet been evaluated on more complex datasets.

As a lower baseline we start with a complete nonsense: we initialize RDF2Vec models with random weights and evaluate them on the DLCC dataset.
On the other hand, as a strong baseline to beat we will use TransE-L2 model. 



\begingroup
\newcommand{\D}{\textemdash{}}
\begin{table}[t]
  \centering
  \caption{RDF2Vec on DLCC (synthetic ontology): binary node classification with a logistic regression on frozen entity vectors. All entries are \textbf{accuracy} on the test set, reported as point estimate $\pm$ bootstrap standard error. Within each setup, columns compare embedding sources.}
  \label{tab:dlcc-rdf2vec}
  \footnotesize
  \setlength{\tabcolsep}{6pt}
  \begin{tabular}{@{}lcc@{}}
    \toprule
    Test case
      & Random Walks
      & WL Kernel Walks \\
    \midrule
    TC01 & 0.867 & \D \\
    TC02 & 0.725 & \D \\
    TC03 & \D & \D \\
    TC04 & \D & \D \\
    TC05 & \D & \D \\
    TC06 & \D & \D \\
    TC07 & 0.552 & \D \\
    TC08 & 0.597 & \D \\
    TC09 & 0.612 & \D \\
    TC10 & 0.630 & \D \\
    TC11 & 0.610 & \D \\
    TC12 & 0.640 & \D \\
    \midrule
    Macro avg.
      & \D & \D \\
    \bottomrule
  \end{tabular}
  \vspace{0.4em}
  \par\noindent\small\textit{Note:} Macro avg.\ is the unweighted mean of accuracy over TC01--TC12 for each column. Replace placeholders with $m \pm \sigma$ once runs are complete.
\end{table}

\endgroup

The DLCC dataset's synthetic ontology is a highly problematic dataset due to the fact that model scores vary significantly across runs.
This is a problem because it makes it difficult to draw conclusions about the performance of RDF2Vec models on the DLCC dataset.
To address this issue, we will run multiple runs of RDF2Vec models on each test case (TC01--TC12).

Nevertheless, we see that RDF2Vec models start to struggle with more complex test cases such as TC07--TC12. 

\section{Pretraining a Protograph for RDF2Vec}

We can notice that when training RDF2Vec models we only use the instance graph and not utilize the ontology graph. 
This is a missed opportunity, as the ontology graph contains valuable information about the structure and semantics of the data. 
To address this, we propose pretraining RDF2Vec models on the ontology graph before fine-tuning them on the instance graph. 
This approach is inspired by the success of pretraining in other domains, such as natural language processing. 

So roughly, injecting schema-based information into the training could help enhance the semantic awareness of KG embeddings \cite{hubert2024treatdifferentnegativesdifferently}.


P1 protograph seems to be absolutely useless judging from the results presented in Table 4 \cite{hubert2024treatdifferentnegativesdifferently}. 
We reference Table 4 specifically because it has results for Node classification task on 12 DLCC synthetic test cases. 

P2 is a little more promising, however it can clearly be seen that it is unstable and sometimes shows ~10\% improvement and sometimes ~10\% degradation.

\subsection{Building a Protograph}
We utilize strategies described in \cite{hubert2024treatdifferentnegativesdifferently} for constructing protographs. The strategies are P1 and P2. 


\subsection{Training on Protograph}

Before starting to train on Protographs, it is important to make create an assumption that we fix the Word2Vec training and Random Walks configurations.
That way we will be able to judge the effect of P1, P2 strategies. To explore the stability we will toggle the random seed and average the metrics. 


So far training on P1 protograph is quite unstable that might be due to the fact that it has too little data. Another drawback of P1 strategy is also pointed out in original paper:
if schema has many abstract classes that do not have any direct instances, then embeddings of these classes will not be trasferred to the instance embeddings. Thus, resulting in inefficient pretraining. 



\subsection{Evaluating the Pretrain Stage}
In this section we will evaluate on the TC's the pretrained models and compare them to each other. 
This requires us to evaluate the pretrained embeddings on the test cases without fine-tuning on the instance graph.
Table~\ref{tab:dlcc-pretrain-stage-eval} compares frozen entity vectors under one fixed walk-generation and Word2Vec configuration: training on instance walks from scratch (\textit{V}) versus the same recipe after protograph pretraining (\textit{P1}, \textit{P2}), each without subsequent fine-tuning on the instance graph, so differences between columns isolate the effect of pretraining.

\begingroup
\newcommand{\D}{\textemdash{}}
\begin{table}[t]
  \centering
  \caption{DLCC synthetic ontology: pretrain-stage evaluation with a \emph{fixed} configuration. Random-walk generation and Word2Vec hyperparameters are identical for all columns; only the initialization/pretraining path differs. }
  \label{tab:dlcc-pretrain-stage-eval}
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}lccc@{}}
    \toprule
    TC
      & V
      & P1
      & P2 \\
    \multicolumn{4}{@{}l@{}}{%
    } \\
    \midrule
    TC01 & \D & \D & \D \\
    TC02 & \D & \D & \D \\
    TC03 & \D & \D & \D \\
    TC04 & \D & \D & \D \\
    TC05 & \D & \D & \D \\
    TC06 & \D & \D & \D \\
    TC07 & \D & \D & \D \\
    TC08 & \D & \D & \D \\
    TC09 & \D & \D & \D \\
    TC10 & \D & \D & \D \\
    TC11 & \D & \D & \D \\
    TC12 & \D & \D & \D \\
    \midrule
    Macro avg.
      & \D & \D & \D \\
    \bottomrule
  \end{tabular}
  \vspace{0.4em}
\end{table}
\endgroup




\subsection{Hyperparameter Tuning}

The initial results of training on P2 and P1 protographs are not as promising as we hoped. Maybe to make it work we need to perform a more careful hyperparameter tuning. 
For that we start a hyperparameter grid search. For each difficult TC (TC07--TC12) we will run 5 runs of RDF2Vec with different hyperparameters and report the best results.

We will tune hyperparameters for 2 stages in the pipeline: (i) walks generation and (ii) Word2Vec training. 
Walks generation hyperparameters include: (a) depth, (b) walks per entity. We include only these parameters because we focus on jRDF2Vec implementation for now. 
We will cover other params later. 

Word2Vec training hyperparameters include: (a) embedding dimension, (b) window size, (c) negative samples, (d) min count, (e) skipgram or CBOW, (f) epochs, (g) learning rate.

Table \ref{tab:dlcc-rdf2vec-protograph} shows the results of RDF2Vec models pretrained on P1 and P2 protographs.

\begingroup
\newcommand{\D}{\textemdash{}}
\begin{table}[t]
  \centering
  \caption{RDF2Vec on DLCC (synthetic ontology) under protograph pretraining: binary node classification with a logistic regression on frozen entity vectors.}
  \label{tab:dlcc-rdf2vec-protograph}
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}lccc@{}}
    \toprule
    Test case
      & No protograph
      & P1
      & P2 \\
    \midrule
    TC01 & 0.867 & \D & \D \\
    TC02 & 0.725 & \D & \D \\
    TC03 & 0.762 & \D & \D \\
    TC04 & 0.997 & \D & \D \\ % Too long to run so we won't add more. Also it's useless beacuse it's a very easy test case.
    TC05 & 0.817 & \D & \D \\
    TC06 & 0.962 & \D & \D \\
    TC07 & 0.605 & 0.590 & 0.640 \\
    TC08 & 0.650 & 0.580 & 0.672 \\
    TC09 & 0.657 & 0.610 & 0.657 \\
    TC10 & 0.630 & 0.597 & 0.670 \\
    TC11 & 0.610 & 0.597 & 0.590 \\
    TC12 & 0.640 & 0.652 & 0.712 \\
    \midrule
    Macro avg.
      & \D & \D & \D \\
    \bottomrule
  \end{tabular}
  \vspace{0.4em}
  \par\noindent\small\textit{Note:} Macro avg.\ is the unweighted mean of accuracy over TC01--TC12 for each column. Replace placeholders with $m \pm \sigma$ once runs are complete.
\end{table}
\endgroup

\subsection{Initialization Strategies for P2 Protograph}
So far when working with P2 protograph we have been initializing the instance embeddings with the corresponding most specific class embedding.
This is a reasonable approach, however it might be the case that it is not the best one.
For that reason we will explore other initialization strategies for P2 protograph.
For example, we can initialize instance embeddings with the average of all class embeddings that are connected to the instance. 
Another approach could be to initialize instance embeddings with the weighted average of all class embeddings, where weights are determined by their distance from the most specific class.

\begingroup
\newcommand{\D}{\textemdash{}}
\begin{table}[t]
  \centering
  \caption{P2 protograph: comparison of instance embedding initialization strategies (binary node classification accuracy on DLCC).}
  \label{tab:p2-init-strategies}
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}lccc@{}}
    \toprule
    TC
      & Most Specific
      & Average
      & Weighted \\
    \midrule
    TC01 & \D & \D & \D \\
    TC02 & \D & \D & \D \\
    TC03 & \D & \D & \D \\
    TC04 & \D & \D & \D \\
    TC05 & \D & \D & \D \\
    TC06 & \D & \D & \D \\
    TC07 & \D & \D & \D \\
    TC08 & \D & \D & \D \\
    TC09 & \D & \D & \D \\
    TC10 & \D & \D & \D \\
    TC11 & \D & \D & \D \\
    TC12 & \D & \D & \D \\
    \midrule
    Macro avg.
      & \D & \D & \D \\
    \bottomrule
  \end{tabular}
  \vspace{0.4em}
  \par\noindent\small\textit{Note:} Macro avg.\ is the unweighted mean of accuracy over TC01--TC12 for each column. Replace placeholders with reported values once runs are complete.
\end{table}
\endgroup

\subsection{Convergence of Protograph Pretrained Model}
We analyze how quickly Word2Vec training on the instance-graph walks reduces loss when embeddings are initialized in three ways:
\textit{V} denotes training from scratch (no protograph pretraining), while \textit{P1} and \textit{P2} denote fine-tuning after protograph pretraining with strategies P1 and P2, respectively.
For each difficult test case TC07--TC10, Figure~\ref{fig:protograph-convergence-loss} plots the \emph{interval} training loss (Gensim cumulative loss difference between consecutive logging steps) against the batch step index during Word2Vec training on the instance corpus.

\begin{figure}[t]
  \centering
  \begin{subfigure}{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{assets/tc07_loss_per_step_v_p1_p2.png}
    \caption{}
  \end{subfigure}\hfill
  \begin{subfigure}{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{assets/tc08_loss_per_step_v_p1_p2.png}
    \caption{}
  \end{subfigure}

  \vspace{0.6em}

  \begin{subfigure}{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{assets/tc09_loss_per_step_v_p1_p2.png}
    \caption{}
  \end{subfigure}\hfill
  \begin{subfigure}{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{assets/tc10_loss_per_step_v_p1_p2.png}
    \caption{}
  \end{subfigure}
  \caption{Interval Word2Vec loss vs.\ training step on the instance walks for TC07--TC10. Curves compare no pretraining (\textit{V}) with fine-tuning after P1 and P2 protograph pretraining.}
  \label{fig:protograph-convergence-loss}
\end{figure}

\subsection{Evaluating Pretrain Stage}
So far we have covered (i) the evaluation of RDF2Vec trained on protographs and on instance graph, 
(ii) convergence of RDF2Vec on instance graph with prograph embeddings initialization.

In order to analyze the pretrained embeddings deeper we evaluate them on DLCC test cases as shown in Table~\ref{tab:dlcc-protograph-embeddings-eval}.
\textit{Random} denotes RDF2Vec trained with a random initialization of embedding weights (no protograph pretraining); \textit{P1} and \textit{P2} use weights initialized from the corresponding protograph pretraining runs before fine-tuning on the instance graph.

\begingroup
\newcommand{\D}{\textemdash{}}
\begin{table}[t]
  \centering
  \caption{Pretrained protograph embeddings evaluated on DLCC: binary node classification with a logistic regression on frozen entity vectors. Accuracy on the test set.}
  \label{tab:dlcc-protograph-embeddings-eval}
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}lccc@{}}
    \toprule
    TC & Random & P1 & P2 \\
    \midrule
    TC01 & \D & \D & \D \\
    TC02 & \D & \D & \D \\
    TC03 & \D & \D & \D \\
    TC04 & \D & \D & \D \\
    TC05 & \D & \D & \D \\
    TC06 & \D & \D & \D \\
    TC07 & \D & \D & \D \\
    TC08 & \D & \D & \D \\
    TC09 & \D & \D & \D \\
    TC10 & \D & \D & \D \\
    TC11 & \D & \D & \D \\
    TC12 & \D & \D & \D \\
    \midrule
    Macro avg.
      & \D & \D & \D \\
    \bottomrule
  \end{tabular}
  \vspace{0.4em}
  \par\noindent\small\textit{Note:} Macro avg.\ is the unweighted mean of accuracy over TC01--TC12 for the Random column; P1/P2 averages are omitted until all test cases are filled. Replace entries with $m \pm \sigma$ once runs are complete.
\end{table}
\endgroup

\section{DBpedia Graph \& Schema Construction Pipeline (DLCC Setup)}

\subsection{Dataset: DBpedia Snapshot (2021-09)}

The following DBpedia resources were used: 

\begin{itemize}
    \item \textbf{Mapping-based object properties (cleaned)} (\textasciitilde171 MB) \\
    Main factual graph \\
    \texttt{mappingbased-objects\_lang=en.ttl}

    \item \textbf{Instance types (specific)} (\textasciitilde42 MB) \\
    Entity type information \\
    \texttt{instance-types\_lang=en\_specific.ttl}

    \item \textbf{DBpedia ontology (N-Triples)} (\textasciitilde5.1 MB) \\
    Schema definition \\
    \texttt{dbpedia\_ontology.nt}
\end{itemize}

\subsection{1. Seed Entity Extraction}

Entities were collected from the DLCC DBpedia dataset across all:
\begin{itemize}
    \item Test cases
    \item Domains
    \item Dataset sizes: 50 / 500 / 5000
\end{itemize}

Processing steps:
\begin{itemize}
    \item Deduplication of entities
    \item Conversion to N-Triples IRI format (\texttt{<...>})
\end{itemize}

\noindent Output: \texttt{seed\_entities.txt}

\subsection{2. 2-Hop Instance Graph Construction}

Using the mapping-based object properties file:

\paragraph{Hop 1}
Extract triples where:
\begin{itemize}
    \item subject $\in$ seed entities OR
    \item object $\in$ seed entities
\end{itemize}
Output: \texttt{hop1.ttl}

\paragraph{Hop 2}
Expand using entities from Hop 1. \\
Output: \texttt{hop2.ttl}

\paragraph{Result}
A localized 2-hop subgraph centered around DLCC entities.

\subsection{3. Type Enrichment}

Using the instance types (specific) file:

\begin{itemize}
    \item Extract \texttt{rdf:type} triples for all entities in the 2-hop graph
\end{itemize}

\noindent Output: \texttt{types\_hop2.ttl}

\subsection{4. Final Instance Graph Construction}

Merge:
\begin{itemize}
    \item \texttt{hop2.ttl} (factual relations)
    \item \texttt{types\_hop2.ttl} (type information)
\end{itemize}

\noindent Output: \texttt{G.ttl} (Final Instance Graph)

\subsection{5. Schema Graph Construction}

Using the DBpedia ontology file:

\begin{itemize}
    \item Extract classes from \texttt{types\_hop2.ttl}
    \item Extract properties from \texttt{G.ttl}
    \item Expand classes via \texttt{rdfs:subClassOf} (up to 3 levels)
\end{itemize}

Retained schema relations:
\begin{itemize}
    \item \texttt{rdfs:subClassOf}
    \item \texttt{rdfs:domain}
    \item \texttt{rdfs:range}
\end{itemize}

\noindent Output: \texttt{schema.ttl}

\subsection{Final Outputs}

\begin{itemize}
    \item \textbf{\texttt{G.ttl}}: 2-hop instance graph with type information
    \item \textbf{\texttt{schema.ttl}}: filtered ontology schema graph
\end{itemize}

\subsection{Usage in RDF2Vec Experiments}

\begin{itemize}
    \item \textbf{Vanilla}: Instance graph only (\texttt{G.ttl})
    \item \textbf{Resume Training}: Continued training on instance graph
    \item \textbf{Schema + Resume}: Pretraining on schema graph (\texttt{schema.ttl}) followed by training on instance graph (\texttt{G.ttl})
\end{itemize}



% 
% What would be the loss function? 
% 
bina

\section{Problems of RDF2Vec}



\section{Conclusions}

% \citet{zobel2004} writes:

\blockcquote{zobel2004}{%
  The closing section, or summary, is used to draw together the topics discussed
  in the paper. It should include a concise statement of the paper's important
  results and an explanation of their significance. This is an appropriate place
  to state (or restate) any limitations of the work: shortcomings in the
  experiments, problems that the theory does not address, and so on.

  The conclusions are an appropriate place for a scientist to look beyond the
  current context to other problems that were not addressed, to questions that
  were not answered, to variations that could be explored. They may include
  speculation, such as discussion of possible consequences of the results.

  A \emph{conclusion} is that which concludes, or the end. \emph{Conclusions}
  are the inferences drawn from a collection of information. Write
  "Conclusions", not "Conclusion". If you have no conclusions to draw, write
  "Summary".}

\bibliography{references}


\appendix
\section{Additional Experimental Results}


\section{Proof Details}


\clearpage
\section*{Ehrenwörtliche Erklärung}

Ich versichere, dass ich die beiliegende Bachelor-, Master-, Seminar-, oder
Projektarbeit ohne Hilfe Dritter und ohne Benutzung anderer als der angegebenen
Quellen und in der untenstehenden Tabelle angegebenen Hilfsmittel angefertigt
und die den benutzten Quellen wörtlich oder inhaltlich entnommenen Stellen als
solche kenntlich gemacht habe. Diese Arbeit hat in gleicher oder ähnlicher Form
noch keiner Prüfungsbehörde vorgelegen. Ich bin mir bewusst, dass eine falsche
Erklärung rechtliche Folgen haben wird.

% Declare below which AI tools you used in the process of writing your work,
% including text, image, code, and data generation. If you used a tool for a
% purpose not included in the list yet, add it to the list.
\begin{center}
  \textbf{Declaration of Used AI Tools} \\[.3em]
  \begin{tabularx}{\textwidth}{lXlc}
    \toprule
    Tool & Purpose & Where? & Useful? \\
    \midrule
    ChatGPT & Rephrasing & Throughout & + \\
    DeepL & Translation & Throughout & + \\
    ResearchGPT & Summarization of related work & Sec.~\ref{sec:related_work} & - \\
    Dall-E & Image generation & Figs.~2, 3 & ++ \\
    GPT-4 & Code generation & functions.py & + \\
    ChatGPT & Related work hallucination & Most of bibliography & ++ \\
    \bottomrule
  \end{tabularx}
\end{center}

\vspace{2cm}
\noindent Unterschrift\\
\noindent Mannheim, den XX.~XXXX 2024 \hfill

\end{document}
